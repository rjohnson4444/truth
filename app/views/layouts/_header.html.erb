<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to root_path, class: "navbar-brand" do %>
        <i class="fa fa-child fa-lg"></i>
        Truth
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
      <ul class="nav navbar-nav navbar-right">
        <!-- Previous if current user statement -->
        <% if current_user %>
        <li><%= link_to "Timeline", activities_path %></li>
        <li><%= link_to "People (#{request_count})", users_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= gravatar_tag current_user.email, size: 20 %> <%= current_user.username %><span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Profile", user_path(current_user.username) %></li>
            <li><%= link_to "Friends", users_path(people: "friends") %></li>
            <li><a href="#"><%= link_to "Settings", edit_user_registration_path %></a></li>
            <li class="divider"></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
            <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>            
          </ul>
        </li>
      </ul>
  </div><!-- /.container-fluid -->
</nav>